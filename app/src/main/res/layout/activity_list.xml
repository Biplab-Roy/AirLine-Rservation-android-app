<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:background="@drawable/front_view"
    tools:context="com.example.biplabroy.airreservation.ListActivity">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="List of Customers"
        android:gravity="center"
        android:textSize="20dp"
        android:textColor="#000000"
        android:layout_marginBottom="10dp"
        android:layout_alignParentTop="true"
        android:id="@+id/tv"
        ></TextView>

    <ListView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@+id/logout"
        android:id="@+id/lv"
        android:layout_below="@+id/tv"
        >

    </ListView>

    <Button
        android:layout_width="150dp"
        android:layout_height="wrap_content"
        android:text="add flight"
        android:id="@+id/add_flignt"
        android:layout_alignParentRight="true"
        android:layout_marginTop="20dp"
        android:layout_above="@+id/logout"
        style="@style/button"
        android:onClick="addFlight"
        />

    <Button
        android:layout_width="150dp"
        android:layout_height="wrap_content"
        android:text="Cancle flight"
        android:id="@+id/cancle_flight"
        android:layout_alignParentLeft="true"
        android:layout_above="@+id/logout"
        android:layout_marginTop="20dp"
        style="@style/button"
        android:onClick="cancleFlight"
        />

    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Log Out"
        android:id="@+id/logout"
        android:layout_alignParentBottom="true"
        android:layout_marginTop="20dp"
        style="@style/button"
        android:onClick="logout"
        />
</RelativeLayout>
